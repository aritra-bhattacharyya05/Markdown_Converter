# MarkItDown Converter

A simple Streamlit application that converts supported document formats to Markdown using Microsoft's MarkItDown.

## Run locally

pip install -r requirements.txt

streamlit run app.py